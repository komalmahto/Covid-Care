/**
 * adds leading 0 to the number when it is lower than 10
 * @private
 */
declare const pad: (n: number) => string;
/**
 * Formats date to YYYY-MM-DD
 *
 * @param   {Date}    date
 * @return  {string}
 * @private
 */
declare const formatDate: (date: Date) => string;
/**
 * Formats date to YYYY-MM-DD HH:mm
 *
 * @param   {Date}    date
 * @return  {string}
 * @private
 */
declare const formatDateTime: (date: Date) => string;
/**
 * Based on the property type formats date to either YYYY-MM-DD HH:mm or YYYY-MM-DD
 *
 * @param   {Date}    date
 * @return  {string}
 * @private
 */
declare const formatDateProperty: (date: Date, propertyType?: "string" | "number" | "boolean" | "password" | "textarea" | "float" | "date" | "datetime" | "mixed" | "reference" | "richtext" | undefined) => string;
export { formatDateProperty, formatDate, formatDateTime, pad, };
